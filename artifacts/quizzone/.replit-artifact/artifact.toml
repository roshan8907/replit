kind = "web"
previewPath = "/"
title = "QuizZone"
version = "1.0.0"
id = "artifacts/quizzone"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23662

[services.development]
run = "pnpm --filter @workspace/quizzone run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/quizzone", "run", "build" ]
publicDir = "artifacts/quizzone/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23662"
BASE_PATH = "/"
